import tkinter


class SidePanel:
    def __init__(self, parent, bot):
        self.parent = parent
        self.bot = bot
        self.parent = parent
        self.bot = bot
        self.FRAME = tkinter.Frame(parent, width=624, height=874, highlightbackground="blue", highlightthickness=3)
        self.FRAME.grid_propagate(False)
